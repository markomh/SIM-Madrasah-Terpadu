SYSTEM CONTEXT & ERROR LOG:The folder permission issue is fixed, but php artisan test still returns 500 Internal Server Error for all Auth and Tenant tests.ROOT CAUSE ANALYSIS:A 500 error in Laravel tests means the application is throwing a PHP exception or SQL error, but the test framework is catching it and returning a generic 500 HTTP response. You are currently flying blind because the real error is hidden.YOUR IMMEDIATE TASKS:Unmask the Error: Open tests/Feature/Auth/AuthTest.php. Inside the login_valid_mengembalikan_token_dan_capabilities test, add $this->withoutExceptionHandling(); at the very beginning of the function.Run the Test: Execute php artisan test --filter login_valid in your environment. Because exception handling is disabled, the test will now crash and output the exact PHP or SQL Exception (e.g., missing column, relation error, database not migrated, or middleware crash).Fix the Core Issue: Based on the stack trace you discover, fix the actual code (Controller, Middleware, Migration, or Test Factory).Hint: Check if your test setup is properly creating the Madrasah entity before creating the Pegawai. Check if the id_madrasah foreign key constraint is failing, or if the SetTenantContext middleware is crashing during login.Verify: Once you find and fix the root cause, remove $this->withoutExceptionHandling(), run php artisan test again, and output the passing result. Do not guess the solution; let the actual exception guide you.